interface:
  display_name: "Rust Middleware"
  short_description: "Middleware composition and request pipeline control"
  default_prompt: "Use $rust-middleware to solve this Rust task with concrete code and checks."
